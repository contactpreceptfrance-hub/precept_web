# Dependencies
node_modules
/.pnp
.pnp.js
.yarn

# Testing
/coverage

# Next.js
# No leading slash: the app lives in nextjs_space/, so these must match at any
# depth. With "/.next/" the build output under nextjs_space/ was being tracked.
.next/
out/
*.tsbuildinfo
next-env.d.ts

# Misc
.DS_Store
*.pem

# Env
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Vercel
.vercel

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.worktrees/

# Local reference material kept at the repo root (Precept brochures, the
# scraped Canadian catalogue) — never meant to ship with the site.
/*.pdf
/Precept Produkt Katalog.html
/Precept Produkt Katalog_files/
# Ad-hoc design screenshots dropped at the root during the 2026-03 redesign.
# Kept on disk as reference, but they are not project assets.
images/Capture d*.png

# Claude Code local settings
.claude/
